language: objective-c
osx_image: xcode8.3

branches:
  only:
    - master
env:
  - LC_CTYPE=en_US.UTF-8 LANG=en_US.UTF-8
script:
  - set -o pipefail
  - xcodebuild ChangeAppIcon.xcodeproj -scheme ChangeAppIcon -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO | xcpretty -c
